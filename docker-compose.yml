# MERL-T Docker Compose - Full Stack
# Usage: docker-compose up -d
#
# This runs the complete stack (backend + frontend + databases).
# For development with native backend/frontend, use docker-compose.dev.yml instead.

version: '3.8'

services:
  # ===========================================================================
  # Backend Service (FastAPI)
  # ===========================================================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: merl-t-backend
    ports:
      - "8000:8000"
    volumes:
      # Mount code for hot-reload during development
      - ./backend:/app/backend
      - ./data:/app/data  # Persist SQLite database
    environment:
      # Database (using SQLite by default, uncomment PostgreSQL section below to use it)
      - DATABASE_URL=sqlite+aiosqlite:///./data/rlcf.db
      # To use PostgreSQL instead:
      # - DATABASE_URL=postgresql+asyncpg://merl_t:merl_t_password@postgres:5432/merl_t_db

      # Server
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=true

      # CORS (allow frontend)
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

      # API Keys (set in .env file or override here)
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-your_openrouter_api_key_here}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-your_admin_api_key_here}

      # RLCF Configuration
      - AUTHORITY_WEIGHT_BASELINE=0.4
      - AUTHORITY_WEIGHT_TRACK_RECORD=0.4
      - AUTHORITY_WEIGHT_RECENT_PERFORMANCE=0.2
      - DISAGREEMENT_THRESHOLD=0.3
      - TRACK_RECORD_LAMBDA=0.95

      # AI Model
      - AI_MODEL=openai/gpt-3.5-turbo
      - AI_TEMPERATURE=0.7
      - AI_MAX_TOKENS=1000

      # Logging
      - LOG_LEVEL=INFO
      - LOG_FORMAT=text

      # Week 6 - Orchestration (Phase 2+)
      - ROUTER_MODEL=${ROUTER_MODEL:-anthropic/claude-3.5-sonnet}
      - ROUTER_TEMPERATURE=${ROUTER_TEMPERATURE:-0.1}
      - EXPERT_MODEL=${EXPERT_MODEL:-anthropic/claude-3.5-sonnet}
      - SYNTHESIZER_MODEL=${SYNTHESIZER_MODEL:-anthropic/claude-3.5-sonnet}
      - MAX_ITERATIONS=${MAX_ITERATIONS:-3}

      # Norma Controller API (Visualex microservice)
      - NORMA_API_URL=http://visualex:5000

      # Sentenze API (placeholder/mock - configure when available)
      - SENTENZE_API_URL=http://localhost:5001

      # Week 7 - Preprocessing & Orchestration Database
      - ORCHESTRATION_DATABASE_URL=postgresql+asyncpg://merl_t:merl_t_password@postgres-orchestration:5432/orchestration_db
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=merl_t_password
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    command: uvicorn backend.rlcf_framework.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
        required: false  # Optional dependency

  # ===========================================================================
  # Visualex API (Phase 2+ - Legal Scraper Microservice)
  # ===========================================================================
  visualex:
    build:
      context: ./visualex
      dockerfile: Dockerfile
    container_name: merl-t-visualex
    ports:
      - "5000:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - RATE_LIMIT=100
      - RATE_LIMIT_WINDOW=60
      - HISTORY_LIMIT=100
      - LOG_LEVEL=INFO
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    profiles:
      - phase2  # Start with Phase 2+ (API Agent depends on this)

  # ===========================================================================
  # KG Ingestion API (Priority 6 - LLM-driven Knowledge Graph Ingestion)
  # ===========================================================================
  ingestion-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: merl-t-ingestion-api
    ports:
      - "8002:8002"
    volumes:
      - ./backend/preprocessing:/app/backend/preprocessing
    environment:
      # LLM & AI
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-your_openrouter_api_key_here}
      - LLM_MODEL=${LLM_MODEL:-anthropic/claude-3.5-sonnet}
      - LLM_TEMPERATURE=${LLM_TEMPERATURE:-0.1}

      # Databases
      - DATABASE_URL=postgresql+asyncpg://merl_t:merl_t_password@postgres-orchestration:5432/orchestration_db
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=merl_t_password
      - REDIS_HOST=redis
      - REDIS_PORT=6379

      # visualex integration
      - VISUALEX_URL=http://visualex:5000

      # Server
      - HOST=0.0.0.0
      - PORT=8002

      # CORS
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173

      # Logging
      - LOG_LEVEL=INFO

    command: python -m backend.preprocessing.api.main
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8002/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: unless-stopped
    depends_on:
      visualex:
        condition: service_healthy
      postgres-orchestration:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      redis:
        condition: service_healthy
    profiles:
      - priority6  # Start with Priority 6: docker-compose --profile priority6 up

  # ===========================================================================
  # Frontend Service (React + Vite)
  # ===========================================================================
  frontend:
    image: node:20-alpine
    container_name: merl-t-frontend
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/rlcf-web:/app
      - /app/node_modules  # Anonymous volume for node_modules
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 3000"
    depends_on:
      - backend
    restart: unless-stopped

  # ===========================================================================
  # PostgreSQL (Optional - uncomment to use instead of SQLite)
  # ===========================================================================
  postgres:
    image: postgres:16-alpine
    container_name: merl-t-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=merl_t
      - POSTGRES_PASSWORD=merl_t_password
      - POSTGRES_DB=merl_t_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U merl_t"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    profiles:
      - postgres  # Only start if explicitly requested: docker-compose --profile postgres up

  # ===========================================================================
  # PostgreSQL Orchestration (Week 7 - separate database for orchestration API)
  # ===========================================================================
  postgres-orchestration:
    image: postgres:16-alpine
    container_name: merl-t-postgres-orchestration
    ports:
      - "5433:5432"  # Different port to avoid conflict with RLCF postgres
    environment:
      - POSTGRES_USER=merl_t
      - POSTGRES_PASSWORD=merl_t_password
      - POSTGRES_DB=orchestration_db
    volumes:
      - postgres_orchestration_data:/var/lib/postgresql/data
      # Mount migration file for auto-initialization
      - ./migrations/001_create_orchestration_tables.sql:/docker-entrypoint-initdb.d/001_create_orchestration_tables.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U merl_t"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    profiles:
      - week7  # Profile for Week 7: docker-compose --profile week7 up

  # ===========================================================================
  # Neo4j (Phase 2+ - Knowledge Graph)
  # ===========================================================================
  neo4j:
    image: neo4j:5.13-community
    container_name: merl-t-neo4j
    ports:
      - "7474:7474"  # Browser UI
      - "7687:7687"  # Bolt protocol
    environment:
      # Authentication
      - NEO4J_AUTH=neo4j/merl_t_password

      # Plugins (APOC for advanced procedures)
      - NEO4J_PLUGINS=["apoc"]

      # Memory settings (for 5,000+ articles)
      - NEO4J_server_memory_heap_initial__size=512m
      - NEO4J_server_memory_heap_max__size=2G
      - NEO4J_server_memory_pagecache_size=1G

      # Database settings (Neo4j 5.x uses initial.dbms.default_database)
      - NEO4J_initial_dbms_default__database=neo4j

      # APOC configuration
      - NEO4J_apoc_export_file_enabled=true
      - NEO4J_apoc_import_file_enabled=true
      - NEO4J_apoc_import_file_use__neo4j__config=true

    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_import:/var/lib/neo4j/import
      - neo4j_plugins:/plugins
      # Note: Schema file will be copied after container starts
      # to avoid permission issues with direct mounts

    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p merl_t_password -d neo4j 'RETURN 1;' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

    restart: unless-stopped
    profiles:
      - phase2  # Only start for Phase 2+: docker-compose --profile phase2 up

  # ===========================================================================
  # Redis (Phase 2+ - Caching Layer)
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: merl-t-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    profiles:
      - phase2  # Only start for Phase 2+: docker-compose --profile phase2 up

  # ===========================================================================
  # Qdrant (Phase 3+ - Vector Database)
  # ===========================================================================
  qdrant:
    image: qdrant/qdrant:latest
    container_name: merl-t-qdrant
    ports:
      - "6333:6333"  # HTTP API
      - "6334:6334"  # gRPC API
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:6333/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    profiles:
      - phase3  # Only start for Phase 3+: docker-compose --profile phase3 up

# =============================================================================
# Volumes
# =============================================================================
volumes:
  # SQLite data (default)
  data:

  # PostgreSQL (RLCF)
  postgres_data:

  # PostgreSQL Orchestration (Week 7)
  postgres_orchestration_data:

  # Neo4j (Phase 2+)
  neo4j_data:
  neo4j_logs:
  neo4j_import:
  neo4j_plugins:

  # Redis (Phase 2+)
  redis_data:

  # Qdrant (Phase 3+)
  qdrant_data:

# =============================================================================
# Networks (default bridge network)
# =============================================================================
networks:
  default:
    name: merl-t-network
    driver: bridge
