[pytest]
# Pytest configuration for MERL-T
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Exclude archived legacy tests
addopts = --ignore=tests/_archive

markers =
    integration: Integration tests requiring external services (FalkorDB, PostgreSQL)
